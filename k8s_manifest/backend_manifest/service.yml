apiVersion: v1
kind: Service
metadata:
  name: sms-backend-service
  namespace: sms_backend
spec:
  selector:
    app: sms-backend
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
  type: ClusterIP