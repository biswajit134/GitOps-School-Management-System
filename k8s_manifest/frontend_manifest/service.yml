apiVersion: v1
kind: Service
metadata:
  name: sms-frontend-service
  namespace: sms-frontend
spec:
  selector:
    app: sms-frontend
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP