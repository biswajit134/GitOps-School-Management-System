apiVersion: v1
kind: Service
metadata:
  name: chai-app-service
  namespace: sms-frontend
spec:
  selector:
    app: chai-app
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP